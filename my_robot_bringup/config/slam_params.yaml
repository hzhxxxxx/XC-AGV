slam_toolbox:
  ros__parameters:

    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    # ===== 修改1: 使用HuberLoss处理异常值（轮子打滑） =====
    ceres_loss_function: HuberLoss  # 从None改为HuberLoss，关键！

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping #localization

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    #map_file_name: test_steve
    # map_start_pose: [0.0, 0.0, 0.0]
    #map_start_at_dock: true

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02 #if 0 never publishes odometry
    map_update_interval: 5.0
    # ===== 修改2: 地图分辨率（已优化过，保持） =====
    resolution: 0.08
    min_laser_range: 0.0 #for rastering images
    # ===== 修改3: 限制激光范围（已优化过，保持） =====
    max_laser_range: 12.0 #for rastering images
    minimum_time_interval: 0.5
    transform_timeout: 1.0
    tf_buffer_duration: 30.
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    enable_interactive_mode: true

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    # ===== 修改4: 降低更新阈值，更频繁更新地图 =====
    minimum_travel_distance: 0.2  # 从0.3降低到0.2米
    minimum_travel_heading: 0.2   # 从0.3降低到0.2弧度
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    # ===== 修改5: 降低匹配阈值，更容易识别特征 =====
    link_match_minimum_response_fine: 0.4  # 从0.1改为0.4（适中值）
    # ===== 修改6: 扩大扫描链接距离（应对漂移） =====
    link_scan_maximum_distance: 4.0  # 从1.5扩大到4.0米，关键！
    # ===== 修改7: 扩大闭环搜索距离（应对漂移） =====
    loop_search_maximum_distance: 8.0  # 从3.0扩大到8.0米，关键！
    do_loop_closing: true   # 360°雷达，开启闭环
    loop_match_minimum_chain_size: 3
    loop_match_maximum_variance_coarse: 2.0
    loop_match_minimum_response_coarse: 0.30
    # ===== 修改8: 降低闭环匹配阈值 =====
    loop_match_minimum_response_fine: 0.40

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.05

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 1.0
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.5
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1
